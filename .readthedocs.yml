# .readthedocs.yml

build:
    image: latest

python:
    version: 3.6
    install:
      - method: pip
        path: .
        extra_requirements:
           - docs
      - method: setuptools
        path: .
