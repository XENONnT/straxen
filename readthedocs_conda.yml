# Conda environment file for readthedocs
# This is just for installing graphviz, which is external to python
name: py36
channels:
  - conda-forge
dependencies:
  - python=3.6
  - graphviz
  - sphinx
  - sphinx_rtd_theme
  - nbsphinx
  - recommonmark
  - pip:
      - graphviz